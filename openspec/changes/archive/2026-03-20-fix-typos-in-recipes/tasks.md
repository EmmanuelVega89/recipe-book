## Implementation Tasks

- [x] 1. Corregir receta 11: `name` "Cafe mono" → "Café mono"; `description` "una cafe muy cargado" → "un café muy cargado"
- [x] 2. Recetas 1–10: añadir espacio entre cantidad y unidad en ingredientes que usan `g`, `ml` o `kg` pegado al número (ej: `200g` → `200 g`, `500ml` → `500 ml`, `1kg` → `1 kg`)
- [x] 3. Verificar que el servidor JSON (`npm run mock`) sigue arrancando correctamente tras los cambios
